import React from "react";
import AppDrawer from "../AppDrawer/AppDrawer";

const Header = () => {
  return (
    <header>
      <h1>MeMesis</h1>
      <AppDrawer />
    </header>
  );
};

export default Header;
